apiVersion: v1
kind: Secret
type: kubernetes.io/tls
metadata:
  name: {{ template "heimdall.fullname" . }}-certs
  labels:
    {{- include "heimdall.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": "pre-install"
    "helm.sh/hook-delete-policy": "before-hook-creation"
data:
{{ ( include "heimdall.gen-certs" . ) | indent 2 }}